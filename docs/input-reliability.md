# 입력 지연과 버튼 고착 수정 (2026-09-25)

## 확인한 결함

- Linux HID gadget은 이전 USB 전송이 진행 중이면 비차단 write에 EAGAIN을 반환한다. 기존 마우스 코드는 이를 버리고, 키보드는 최종 상태만 재시도하거나 연결 종료 시 해제 보고를 버릴 수 있었다. 소켓 역압으로 오른쪽 버튼 해제 유실을 재현했다.
- 실제 장치 hid.GS2 descriptor는 버튼 usage 1..5를 사용한다. 오른쪽 버튼은 0x02인데 기존 코드는 절대 마우스에 0x04를 보냈다. 동일 클릭을 상대·절대 USB 장치 양쪽으로 중복 전송했다.
- 게이트웨이가 절대/상대 포인터 상태를 따로 관리하여 입력 방식이 바뀌면 누른 버튼이 바뀔 수 있었다. 350ms 강제 해제는 정상적인 길게 누르기와 드래그도 끊었다.
- agent는 TCP payload 전체를 기다리는 동안 HID 재시도를 못 했다. Unicode 키 사이 sleep도 같은 수신 루프를 막았다.
- Progressive 프레임 전송 간격은 40ms인데 대기 계산은 10ms였다. 배포 전 운영 로그에서 5초당 약 33만~54만 번의 flush 시도를 관측했다. 이는 입력과 화면 처리를 공유하는 스레드의 불필요한 부하다.

## 변경

키보드와 포인터 각각 순서를 보존하는 유한 큐를 사용한다. 상대·절대 USB 마우스 보고는 같은 포인터 큐를 사용하고, 다른 USB 장치로 넘어가기 전에 이전 전송 완료를 기다린다. EAGAIN은 실패로 버리지 않고 POLLOUT을 기다린다. 새 입력 없이도 마지막 해제를 전송한다. 버튼 변화가 없는 인접 절대 이동만 합친다. 왼쪽·오른쪽·가운데 버튼과 세로 휠은 상대 USB 장치가 소유하며 게이트웨이 두 포인터 콜백이 상태를 공유한다. 상대 이동 후 버튼 해제 시 이전 절대 좌표를 다시 보내지 않는다. 추가 버튼 4/5와 지원되는 가로 휠만 절대 장치를 사용한다. 상대 이동량은 잘라 버리지 않고 여러 보고로 나눈다.

종료, 연결 해제, 큐 초과, USB 전송 실패 시 오래된 동작을 취소하고 중립 보고를 모든 장치에 재시도한다. 1초 동안 진행이 없는 전송도 같은 복구 경로를 사용한다. 정상적으로 누르고 있는 키와 버튼에는 임의의 시간 제한을 걸지 않는다.

TCP 수신은 부분 프레임을 유지하는 비차단 파서와 최대 32개 메시지 배치를 사용한다. Unicode 키 간격과 클립보드 전달 순서는 출력 큐에서 관리한다. 제어 소켓 쓰기에는 100ms 제한을 두고, 전송 중 실패한 TCP 연결은 닫아 프레임 경계가 깨진 연결을 재사용하지 않는다. 게이트웨이 대기 계산은 실제 코덱 간격을 따르며 입력을 먼저 읽고 화면을 전송한다.

## 검증과 관측

`ctest --test-dir build/host-unit --output-on-failure`로 기존 단위/RTP 테스트와 입력 회귀 테스트를 실행한다. `nanokvm-input-reliability-test`는 USB 역압을 소켓으로 모사한다. 버튼 매핑과 혼합 입력, 마지막 해제, 이동 병합, 수정키 순서와 원형 큐 재사용, 전체 해제, 초과/정지 복구, Unicode 중 클릭, 연속 이모지 붙여넣기 순서, 휠 및 분할 TCP 수신을 검증한다. 실제 키보드나 마우스를 조작하지 않는다.

agent는 5초마다 INPUT 누적 이벤트/전송/재시도/오류/초과 횟수, 키보드/포인터 대기 개수, 최근 구간 최대 큐 대기 및 핸들러 시간을 기록한다. 입력 문자 내용은 기록하지 않는다. 큐 대기 시간에는 의도적인 Unicode 키 간격도 포함되며 USB 호스트 수신 완료나 화면 반영까지의 종단 지연은 아니다. EAGAIN 재시도 횟수 자체는 정상일 수 있다. 오류·초과 증가, 대기 큐가 계속 남는 현상을 점검한다.

자동 회귀 테스트는 확인한 코드 결함의 재발을 검증한다. 일상 사용의 모든 기기·네트워크 조건에서 불편이 사라졌다는 증명과는 구분한다. 특히 USB 자체 분리나 RDP 클라이언트가 보내지 않은 키 해제는 서버가 즉시 알 수 없다.

참고: [Linux 5.10 f_hid 구현](https://github.com/torvalds/linux/blob/v5.10/drivers/usb/gadget/function/f_hid.c), [FreeRDP 3.14 입력 플래그](https://github.com/FreeRDP/FreeRDP/blob/3.14.0/include/freerdp/input.h).

## 배포 확인

- 코드: `d2663df` (기반 수정 `8b7d2e8`). 게이트웨이 CI 실행 `36147955570` 성공.
- 게이트웨이: `dev-1790346652`, 실행 digest `sha256:c6bde679e32afa9053eeb8e35ff2db9e2ad349e5c44f1c11012f16ac4790b96e`. GitOps `ae6f13f`, ArgoCD Synced/Healthy, 새 Pod 재시작 0회.
- 에이전트: 실제 실행 파일 SHA-256 `d6a845884a2ffdeadae926464f84f48dca8cf09933b270e2841d126f0d8bdcf8`와 빌드 파일 일치. 교체 전 파일은 장치의 `nanokvm-agent.before-input-fix-20260925`에 보관했다.
- 로컬 CTest 3개 대상 통과, 입력 회귀 10개 시나리오를 실제 RISC-V 장치에서도 통과했다. 입력 회귀의 AddressSanitizer/UndefinedBehaviorSanitizer 검사도 통과했다.
- 25초 동안 조작 없는 FreeRDP Sample 클라이언트로 Progressive 전송을 확인했다. 안정 구간의 flush 증분은 5초당 178/180/192회였고, 같은 구간 프레임 증분은 115/111/113개였다. 배포 전 운영 연결의 5초당 수십만 회 반복이 사라졌다. 클라이언트가 다르므로 이 비교를 사용자의 종단 입력 지연 개선율로 해석하지 않는다.
- 시험 연결 종료의 해제 처리까지 agent `errors=0`, `overflow=0`, `pending=0/0`, 처리 구간 `max_handle_ms=1`을 확인했다. 이 관측 구간에는 실제 사용자 타이핑/클릭이 없었으므로 일상 사용의 장시간 무재발을 입증한 것은 아니다.

## Control+Space 이후 멈춤 추가 조사 (2026-09-25)

사용자가 전환 직후 약 3초 멈춤을 다시 보고했다. 보고 당시 agent의 전송 큐 대기는 최대 1ms, 입력 처리 시간은 최대 2ms였고 전송 오류·재시도는 0이었다. 이는 USB의 양방향 처리가 정상이라는 뜻은 아니었다.

키보드 descriptor는 LED Output report를 선언하지만 에이전트는 `/dev/hidg0`를 쓰기 전용으로만 사용했다. 실제 장치에서 읽기 전용 비차단 진단을 실행하자 즉시 읽을 데이터가 있었고 1초 안에 LED 보고 39개(37개 0x00, 2개 0x02)를 읽었다. Linux HID gadget은 OUT 요청을 userspace가 읽은 뒤에 다시 큐에 넣는다. 읽지 않는 결함은 호스트의 키보드 상태 동기화를 지연시킬 수 있다. 이 결함은 실장치에서 확인했지만, 보고된 3초 멈춤과의 인과관계는 전환 재현으로 추가 확인해야 한다.

수정은 키보드 피드백 전용 비차단 FD를 유지하고, 입력이 없어도 POLLIN을 감시해 HID 루프에서 최대 32개씩 읽는 것이다. 키 전송과 피드백 수신을 분리해 한 방향의 역압이 다른 방향을 막지 않게 했다. `feedback`, `feedback_errors`, `leds` 진단 항목으로 수신 상태를 확인한다. Control+Space의 키 매핑이나 눌림 시간은 바꾸지 않았다.

새 회귀 테스트는 Control+Space 50회와 호스트 LED 응답, 다음 키가 없는 상태의 피드백 수신, 역방향 큐 포화 후 재전송, 키 전송이 막힌 상태의 독립 수신을 검사한다. 수정 전에는 입력이 없는 때 피드백 FD를 감시하지 않는 assertion이 실패했다.

## 오른쪽 클릭 고착 재보고와 진단 보강 (2026-09-26)

00:41 KST 연결 초기화 직전까지의 RDP 세션 기록에는 왼쪽 버튼 누름/해제 각 60개와 가운데 버튼 누름/해제 각 2개가 있었다. 오른쪽 버튼 이벤트는 없었으며, 기록으로 계산한 최종 버튼 상태는 0이었다. 이 결과는 사용자가 본 증상을 부정하거나 USB 호스트의 버튼 상태가 정상임을 입증하지 않는다. 기존 로그에 Control 상태가 빠져 있어, Control이 남아 macOS의 왼쪽 클릭이 보조 클릭으로 처리되는 경우를 구분할 수 없었다.

게이트웨이는 이제 모든 수정키의 누름/해제, Control+Space의 Space 누름/해제, 클릭 당시의 버튼 및 수정키 상태를 기록한다. 이 상태는 RDP에서 받은 상태이며, USB 호스트가 실제로 처리한 상태는 아니다. agent도 Control 수신 시점과 처리 후 수정키/대기 상태를 기록하고, 5초 간격 INPUT 지표에 단조 시계 `at_ms`, `modifiers`, `buttons`를 포함한다. 일반 입력 문자 내용은 기록하지 않는다. 두 프로세스의 단조 시계 기준점은 다르므로 `at_ms`를 서버 시각과 직접 빼서 종단 지연으로 계산하지 않는다.

장치 SSH 인증이 일시적으로 거부된 뒤 기존 인증으로 다시 접속되었다. 실제 실행 중인 agent는 LED 피드백 수정 빌드였고, 장치 로그에서 입력 1,680건의 `retry/errors/overflow/feedback_errors`는 모두 0, 최대 큐 대기는 1ms, 최대 핸들러 시간은 4ms였다. `/dev/hidg*`를 연 프로세스는 agent 하나였다. 이는 userspace에서 USB 전송을 제출하기까지의 관측이다. 호스트의 최종 수신·입력기 처리 완료나 Control+Space 이후 무재발은 아직 확인되지 않았다. 실제 RDP 연결에서 다시 발생하는 구간을 양쪽 진단으로 대조해야 한다.

## 수동 재현: 오른쪽 Ctrl 해제 누락 (2026-09-26, 수정 배포 전)

01:13:19 KST에 오른쪽 Ctrl 누름(`raw=0x1D extended=1 release=0`)을 받은 뒤 해제 이벤트가 없었다. 01:14:00~03의 왼쪽 클릭은 누름/해제가 모두 도착했지만 수정키는 `0x10`이었다. 01:14:04 왼쪽 Ctrl을 눌렀다 떼어도 `0x11 → 0x10`으로 오른쪽 Ctrl은 남았다. agent도 같은 상태를 기록했고, 사용자는 클릭할 때 이미 Ctrl에서 손을 뗐다고 확인했다. macOS의 Ctrl+왼쪽 클릭이 보조 클릭으로 처리되는 증상과 부합한다. 클라이언트가 해제 이벤트를 보내지 않은 원인과 3초 정지의 원인이 동일한지는 아직 입증하지 않았다.

별도로 프로토콜 복구 결함을 확인했다. 서버에는 `SynchronizeEvent` 수신 콜백이 없었으며, 등록된 `FocusInEvent`는 FreeRDP의 클라이언트 전송용 콜백이다. [RDP TS_SYNC_EVENT 규격](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpbcgr/6c5d0ef9-4653-4d69-9ba9-09ba3acd660f)은 모든 키를 해제한 뒤 후속 누름 이벤트로 실제 눌린 상태를 복원하도록 정의한다. [FreeRDP 3.14 입력 구현](https://github.com/FreeRDP/FreeRDP/blob/3.14.0/libfreerdp/core/input.c)의 서버 수신 경로도 `SynchronizeEvent`를 호출한다.

로컬 수정은 이 수신 콜백을 등록하고 새 제어 메시지 `SYNCHRONIZE=16`으로 순서대로 중립 HID 보고를 보낸다. 장애 복구용 `RELEASE_ALL`과 달리 USB 대기 큐를 취소하지 않아 동기화 직전 타이핑을 보존한다. 새 회귀 시나리오는 USB 역압 중 오른쪽 Ctrl 해제 누락, Space 누름/해제 보존, 마우스 해제, 동기화 뒤 수정키 재전송 순서를 검증한다. 로컬 CTest 3개 대상(입력 12개 시나리오)과 게이트웨이·RISC-V agent 빌드가 통과했다.

사용자의 수동 테스트 중에는 세션 조작과 배포를 보류했다. 새 메시지는 기존 agent가 처리하지 않으므로 다음 배포는 agent를 먼저 갱신한 뒤 게이트웨이를 갱신해야 한다. Windows App의 실제 동기화 수신, 포커스 복귀 후 Ctrl 고착 해소, Control+Space 후 정지 재현 여부는 아직 검증하지 않았다.

## 재현 시점 로그 보존

`scripts/collect-input-diagnostics.py --output <directory>`는 실행 중인 서비스를 변경하지 않고 Kubernetes 게이트웨이 로그와 SSH agent 로그를 수집한다. 연결 종료 시 5초 후 다시 연결하며 각 파일은 10MiB씩 현재 파일 포함 5개를 보관한다. `received_utc`는 수집기에서 줄을 받은 시간이고, 재접속 직후 과거 로그에도 붙으므로 실제 발생 시간으로 해석하지 않는다. 게이트웨이의 Kubernetes 타임스탬프와 agent의 단조 시각을 함께 사용한다. SSH 연결 시작에 기기 UTC 시각과 uptime을 기록한다. 재접속 시 일부 기록이 중복될 수 있고 컴퓨터 절전·종료 동안 수집은 보장하지 않는다. 수집기 PID는 출력 디렉터리의 `collector.pid`이며 SIGTERM으로 종료한다. 비밀번호 인증이 필요하면 외부에서 `SSHPASS`를 제공한다.

2026-09-26 01:22 KST부터 `/Users/yangs/Library/Logs/nanokvm-input`에 두 소스의 기록이 실제 추가되는 것을 확인했다. 이는 기존 운영 로그의 지속 보관이며 서비스 재시작이나 새 수정 배포를 하지 않았다.

다음 배포용 코드는 agent 입력 처리 전후 수정키·버튼 상태, 이벤트 순번, 대기 수와 처리 시간을 기록한다. HID 계층은 USB write 성공 시 수정키/버튼 바이트 변화 및 50ms 이상 대기한 보고, 전송 실패와 1초 정지를 기록한다. USB 제출 성공은 호스트의 최종 입력 처리 완료를 의미하지 않는다. 일반 키 코드, 문자, 붙여넣기 내용은 새 진단 로그에 포함하지 않는다. 이 상세 로그 코드는 로컬 테스트 및 양쪽 빌드 통과 상태이며 아직 운영에 적용하지 않았다.
